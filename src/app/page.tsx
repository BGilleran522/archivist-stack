export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui", padding: 40, maxWidth: 640 }}>
      <h1>archivist-stack</h1>
      <p>The substrate is wired. Rename the entity nouns, add your status
      values, write an ingest adapter, name your persona, and tune
      <code> src/config/calibration.ts</code>.</p>
    </main>
  );
}
